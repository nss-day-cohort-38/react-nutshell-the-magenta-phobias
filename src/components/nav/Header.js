import React from "react"

const Header = props => {
    return (
        <>
        <header>
        <h1 className="title">{'{'}fōlô{'}'}</h1>
        </header>
        </>
    )
}

export default Header;